import React from 'react';
import ToHome from './ToHome';
import TextBox from './TextBox';

const About = () => (
  <div>
    <ToHome/>
    <TextBox>This is an about page</TextBox>
  </div>
);

export default About;
